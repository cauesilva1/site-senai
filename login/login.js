function enviar(){
    if (form.nome.value == "")
        {alert ("Não há texto escrito") 
    } else if(form.senha.value ==""){
        alert("não há senha digitada")
    } else{
        alert ("cadastro concluido com sucesso")
    }
}